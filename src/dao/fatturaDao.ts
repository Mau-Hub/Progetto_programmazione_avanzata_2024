/*import {
  Fattura,
  FatturaAttributes,
  FatturaCreationAttributes,
} from '../models/fattura';

export interface parcheggioDao<Fattura, FatturaAttributes.id> {
  create(item: Fattura): Promise<Fattura>;
    findById(id: FatturaAttributes.id): Promise<Fattura | null>;
    findAll(): Promise<Fattura[]>;
    update(id: FatturaAttributes.id, item: Fattura): Promise<boolean>;
    delete(id: FatturaAttributes.id): Promise<boolean>;
  }
  */